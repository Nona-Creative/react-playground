import assert from 'assert'
import parametrize from 'js-parametrize'
import * as R from 'ramda'

import SUT from './CharacterEquipment.selector'
import { itemTypes } from '../Items/Items.constants'

describe('CharacterEquipment selector', () => {
  describe('characterEquipment', () => {
    // TODO: should select character equipped weapon as expected
    // TODO: should select character equipped shield as expected
    // TODO: should select character equipped accessories as expected
    // TODO: should select character equipped attire as expected
  })
})
