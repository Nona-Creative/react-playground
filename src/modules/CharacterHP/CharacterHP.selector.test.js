import assert from 'assert'

import SUT from './CharacterHP.selector'

describe('CharacterHP selector', () => {
  describe('CharacterHP', () => {
    // TODO: should select character attributes as expected
  })
})
